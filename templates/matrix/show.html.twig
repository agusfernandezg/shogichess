{% extends 'base.html.twig' %}

{% block title %}Matrix{% endblock %}

{% block body %}
    <h1>Matrix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matrix.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ matrix.name }}</td>
            </tr>
            <tr>
                <th>Row</th>
                <td>{{ matrix.row }}</td>
            </tr>
            <tr>
                <th>Col</th>
                <td>{{ matrix.col }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ matrix.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('matrix_index') }}">back to list</a>

    <a href="{{ path('matrix_edit', {'id': matrix.id}) }}">edit</a>

    {{ include('matrix/_delete_form.html.twig') }}
{% endblock %}
