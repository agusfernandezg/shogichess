{% extends 'base.html.twig' %}

{% block title %}Move{% endblock %}

{% block body %}
    <h1>Move</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ move.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ move.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('move_index') }}">back to list</a>

    <a href="{{ path('move_edit', {'id': move.id}) }}">edit</a>

    {{ include('move/_delete_form.html.twig') }}
{% endblock %}
